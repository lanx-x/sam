{
  "name": "longwang",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter web --filter cms run dev",
    "dev:web": "pnpm --filter web dev",
    "dev:cms": "pnpm --filter cms dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "start:web": "pnpm --filter web start"
  }
}
